@model IEnumerable<SchoolProject.Models.AnotacoesModel>


<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.texto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.texto)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.cod_anotacao },new { @class="btn btn-success"}) |
            @Html.ActionLink("Detalhes", "Details", new { id = item.cod_anotacao, }, new { @class = "btn btn-warning"}) |
            @Html.ActionLink("Delete", "Delete", new { id=item.cod_anotacao }, new { @class = "btn btn-danger" })
        </td>
    </tr>
}

</table>
